---
import { Image } from "astro:assets";
interface Props {
	src: string;
	alt: string;
}
const {src, alt} = Astro.props;
---

<Image src={src} alt={alt} width="100" height="50" class="object-contain w-24 h-12 inline-block" />
